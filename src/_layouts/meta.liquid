<!DOCTYPE html>
<html
  lang="en"
  data-theme="light"
  id="top">
  {% include meta-head %}
  <body
    {% if pageClass %}
    class="template-{{ pageClass }}"
    {% endif %}>
    <a href="#content" class="a11y-jump sr-only">
      Jump directly to main content
    </a>

    {% render iconset.html %}

    {{ content }}

    {% render page-scripts %}
  </body>
</html>