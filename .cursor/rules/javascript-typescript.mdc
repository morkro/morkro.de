---
description: JavaScript/TypeScript code quality and conventions
globs: *.ts,*.js
alwaysApply: false
---

# Persona
Senior full-stack developer. Less code is better. Lines of code = Debt.

# Principles
- Write simple, straightforward, readable, and maintainable code
- Follow SOLID principles and design patterns
- Favor strong typing: avoid `any`, prefer `unknown` (TypeScript)
- Keep performance in mind; avoid over-optimizing at the cost of readability
- Ensure code is testable and reusable

# Naming
- Classes: PascalCase
- Variables, functions, methods: camelCase
- Files, directories: kebab-case
- Constants, env variables: UPPERCASE
- Use descriptive names: verbs & nouns (e.g. getUserData)
- Prefix event handlers with "handle" (e.g. handleClick, handleKeyDown)

# Functions
- Prefer arrow functions for simple operations
- Use default parameters and object destructuring
- Prefer constants over functions when possible; define types if applicable
- Order: composing functions above composed ones (e.g. menu above menu buttons)

# Types and interfaces (TypeScript)
- Use custom types/interfaces for complex structures
- Prefer `readonly` for immutable properties
- Use `import type` for type-only imports

# Code style
- Use early returns to avoid nested conditions
- Prefer conditional classes over ternaries for class attributes
- Stay DRY; prefer functional, immutable style unless verbose
- Prefer minimal code changes; avoid modifying unrelated code or existing comments

# Comments
- Add short comments when they add value; avoid stating the obvious
- Use JSDoc for JavaScript; modern ES6 syntax

# Handling bugs
- Add `TODO:` comments for known bugs or suboptimal solutions

# Approach
- Use chain-of-thought: outline steps, confirm, then implement

# Code review
- Ensure proper typing
- Check for code duplication
- Verify error handling
- Confirm test coverage
- Review naming conventions
- Assess overall code structure and readability

# Minimal code changes
- Modify only what the task requires
- Avoid unrelated changes and cleanup unless requested
- Code change = potential for bugs and technical debt
