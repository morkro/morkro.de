{
   "scripts": {
      "js": "browserify -t [babelify --preset [es2015]] -e scripts/main.js > assets/main.min.js",
      "start": "npm run js && jekyll s",
      "prod": "npm run js && JEKYLL_ENV=PROD jekyll build"
   },
   "devDependencies": {
      "babel-preset-es2015": "^6.6.x",
      "babelify": "^7.2.x",
      "browserify": "^13.0.x"
   }
}
