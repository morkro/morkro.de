language: node_js
branches:
  only:
     - redesign
node_js:
  - 4.4.0
before_install:
  - rvm install 2.2.2
  - gem install sass
install:
  - npm install
script:
  - npm start
after_success:
  "curl -T index.html -u $FTP_USER:$FTP_PASSWORD ftp://$FTP_HOST/$FTP_PATH"